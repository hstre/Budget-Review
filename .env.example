# Never commit a real secret. GitHub Actions reads the repository secret of
# the same name; local runs may export it in the shell or use .env.local.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
